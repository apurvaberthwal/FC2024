package Strings;

public class TextReplace {
    public static String replaceSubstring(String text, String searchString, String replaceString) {
        return text.replace(searchString, replaceString);
    }
}
